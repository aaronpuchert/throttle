[Unit]
Description=Temperature-based CPU throttling

[Service]
ExecStartPre=/usr/bin/mkfifo PIPE
ExecStartPre=/usr/bin/chown root:users PIPE
ExecStartPre=/usr/bin/chmod 660 PIPE
ExecStart=PREFIX/sbin/throttle CONFIG_FILE PIPE
ExecStopPost=/usr/bin/rm -f PIPE

[Install]
WantedBy=multi-user.target
